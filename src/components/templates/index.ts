// Templates - Page-level layouts that provide structure for organisms
// These define the overall layout and placement of organisms on pages

// Layout Templates
export { ClientWrapper } from '../layout/client-wrapper'
export { AppShell } from '../layout/app-shell'

// Page Templates
// These would be added as we create more template components

// Layout Components that act as templates
export { ThemeProvider } from '../providers/ThemeProvider'

// Note: Page-specific layouts are typically in the app directory
// as layout.tsx files following Next.js 15 App Router conventions 